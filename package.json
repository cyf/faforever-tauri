{
  "name": "faforever",
  "private": true,
  "version": "0.0.0+1",
  "type": "module",
  "scripts": {
    "build": "tauri build",
    "dev": "tauri dev",
    "format": "yarn format:app && yarn format:docs",
    "format:app": "cd app && yarn format && cd ..",
    "format:docs": "cd docs && yarn format && cd ..",
    "postinstall": "npm-run-all install:*",
    "install:app": "cd app && yarn && cd ..",
    "install:docs": "cd docs && yarn && cd ..",
    "prepare": "husky install"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.8.0",
    "@commitlint/config-conventional": "^17.8.0",
    "@tauri-apps/cli": "^1.5.4",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.2",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": "^18"
  }
}
