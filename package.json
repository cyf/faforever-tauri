{
  "name": "faforever",
  "private": true,
  "version": "0.0.0+1",
  "type": "module",
  "scripts": {
    "build": "tauri build",
    "dev": "tauri dev",
    "format": "cd app && yarn format && cd ..",
    "postinstall": "cd app && yarn && cd ..",
    "prepare": "husky install"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.8.0",
    "@commitlint/config-conventional": "^17.8.0",
    "@tauri-apps/cli": "^1.5.2",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.1"
  },
  "engines": {
    "node": "^18"
  }
}
